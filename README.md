# Fetchlinks Webapp

This repository has moved into the main Fetchlinks monorepo:

https://github.com/poptart-sommelier/fetchlinks

The active Next.js web app now lives under `web/` in that repository. Deployment
examples from this repository were ported to `deploy/` in the monorepo and
updated for the combined project layout.

This repository is kept only as a historical reference.
